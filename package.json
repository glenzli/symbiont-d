{
  "name": "symbiont-d-web",
  "private": true,
  "scripts": {
    "build:web": "esbuild web/rich-text-source.js --bundle --format=esm --minify --loader:.woff2=dataurl --loader:.woff=dataurl --loader:.ttf=dataurl --outfile=web/rich-text.js"
  },
  "dependencies": {
    "dompurify": "^3.2.6",
    "katex": "^0.16.22",
    "marked": "^15.0.12"
  },
  "devDependencies": {
    "esbuild": "^0.25.5"
  }
}
